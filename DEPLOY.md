# 🚀 Инструкция по деплою

## Деплой на Render.com (Рекомендуется)

### Шаг 1: Push в GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin [URL-РЕПОЗИТОРИЯ]
git push -u origin main
```

### Шаг 2: Создать приложение на Render

1. Зайти на https://render.com
2. Нажать **"New +"** → **"Web Service"**
3. Подключить GitHub репозиторий
4. Настройки:
   - **Name:** liron-video-shop
   - **Runtime:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Instance Type:** Free

### Шаг 3: Deploy!

Нажать **"Create Web Service"** и ждать ~5 минут.

### Шаг 4: Готово!

Ваш сайт будет доступен по адресу:
```
https://liron-video-shop.onrender.com
```

---

## ⚙️ Переменные окружения (если нужны)

В Render → Settings → Environment:

```
NODE_ENV=production
PORT=3000
```

---

## 📁 Важно для продакшена:

### 1. Видео файлы

⚠️ **Проблема:** Render удаляет загруженные файлы при каждом деплое.

**Решение:** Использовать облачное хранилище:
- **Cloudinary** (бесплатно до 25GB)
- **AWS S3** (платно, но надежно)
- **Backblaze B2** (дешевле S3)

### 2. База данных

Сейчас используется `products.json` (файл).

Для продакшена рекомендуется:
- **MongoDB Atlas** (бесплатно до 512MB)
- **PostgreSQL** (Render предоставляет)

---

## 🔧 Альтернативные варианты деплоя

### Railway.app

```bash
npm i -g @railway/cli
railway login
railway init
railway up
```

### Fly.io

```bash
fly launch
fly deploy
```

### VPS (DigitalOcean, Hetzner)

```bash
ssh root@your-server-ip
git clone [your-repo]
cd liron-video-shop
npm install
npm start
```

---

## 📊 После деплоя проверить:

- ✅ Главная страница открывается
- ✅ Админка требует пароль
- ✅ Загрузка видео работает
- ✅ API отдает товары
- ✅ HTTPS работает

---

## 🆘 Проблемы?

### Сайт не открывается
- Проверить логи в Render
- Убедиться что порт правильный (process.env.PORT)

### Видео не загружаются
- Проверить размер файлов (макс 100MB)
- Проверить права на папку videos/

### 500 ошибка
- Открыть логи: Render → Logs
- Проверить что все зависимости установлены

---

## 🎉 Готово!

После деплоя:
- Главный сайт: `https://your-app.onrender.com/`
- Админка: `https://your-app.onrender.com/admin`
- Пароль: `Liron3214`


